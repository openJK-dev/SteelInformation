项目介绍：
本项目以废钢网的信息为根源，通过爬虫技术获取相应的链接及需要的内容，支持种类、区域、时间等条件的混合搜索


项目问题（导致软件无法继续迭代）：
迭代版本，当时想做一个本地保存数据，然后通过数据分析将分析的结果以图标的形式展示给用户。当时的想法是不同的种类建一张表，不过后来发现不同种类里不同区域的表的字段数也不同（因此可能要继续细分，不过目前只是以种类建表所以就不好继续做下去），所以目前版本的迭代就停在了这里
本地同步的代码在DownLoadService中
